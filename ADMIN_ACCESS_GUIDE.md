# 后台管理系统访问指南

## 📍 后台管理系统位置

后台管理系统位于项目的 `admin` 目录下：

```
app-84zvdc9gufwh/
├── admin/
│   ├── index.html      # 后台管理界面
│   ├── admin.js        # 后台管理逻辑
│   └── README.md       # 使用说明
├── scripts/
│   └── setup-admin.sh  # 配置脚本
└── ...
```

---

## 🚀 快速开始

### 方法一：自动配置（推荐）

**1. 运行配置脚本**
```bash
cd /workspace/app-84zvdc9gufwh
chmod +x scripts/setup-admin.sh
./scripts/setup-admin.sh
```

**2. 启动本地服务器**
```bash
cd admin
python3 -m http.server 8080
```

**3. 访问后台管理系统**
```
浏览器打开：http://localhost:8080
```

---

### 方法二：手动配置

**1. 查看Supabase配置**
```bash
cat .env | grep SUPABASE
```

你会看到类似这样的输出：
```
TARO_APP_SUPABASE_URL=https://xxxxx.supabase.co
TARO_APP_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**2. 编辑 admin/admin.js**

打开 `admin/admin.js` 文件，修改第2-3行：

```javascript
// 修改前
const SUPABASE_URL = 'YOUR_SUPABASE_URL'
const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY'

// 修改后（替换为你的实际值）
const SUPABASE_URL = 'https://xxxxx.supabase.co'
const SUPABASE_ANON_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...'
```

**3. 启动本地服务器**

选择以下任一方式：

**使用Python**：
```bash
cd admin
python3 -m http.server 8080
```

**使用Node.js**：
```bash
cd admin
npx http-server -p 8080
```

**使用PHP**：
```bash
cd admin
php -S localhost:8080
```

**4. 访问后台管理系统**
```
浏览器打开：http://localhost:8080
```

---

## 🌐 部署到线上

### 方法一：Vercel部署（推荐）

**1. 安装Vercel CLI**
```bash
npm install -g vercel
```

**2. 部署admin目录**
```bash
cd admin
vercel
```

**3. 按提示完成部署**
- 选择项目名称
- 确认部署设置
- 获得在线访问地址

**4. 访问后台管理系统**
```
https://your-project.vercel.app
```

---

### 方法二：Netlify部署

**1. 安装Netlify CLI**
```bash
npm install -g netlify-cli
```

**2. 部署admin目录**
```bash
cd admin
netlify deploy --prod
```

**3. 访问后台管理系统**
```
https://your-project.netlify.app
```

---

### 方法三：传统服务器部署

**1. 上传文件**

将 `admin` 目录上传到你的Web服务器：
```bash
# 使用SCP
scp -r admin/ user@your-server:/var/www/html/admin/

# 或使用FTP工具上传
```

**2. 配置Web服务器**

**Nginx配置示例**：
```nginx
server {
    listen 80;
    server_name admin.yourdomain.com;
    
    root /var/www/html/admin;
    index index.html;
    
    location / {
        try_files $uri $uri/ =404;
    }
}
```

**Apache配置示例**：
```apache
<VirtualHost *:80>
    ServerName admin.yourdomain.com
    DocumentRoot /var/www/html/admin
    
    <Directory /var/www/html/admin>
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>
```

**3. 访问后台管理系统**
```
http://admin.yourdomain.com
```

---

## 🔐 安全建议

### 1. 添加访问控制

**使用Nginx基本认证**：
```bash
# 创建密码文件
htpasswd -c /etc/nginx/.htpasswd admin

# 在Nginx配置中添加
location / {
    auth_basic "管理员登录";
    auth_basic_user_file /etc/nginx/.htpasswd;
}
```

**使用Apache基本认证**：
```apache
<Directory /var/www/html/admin>
    AuthType Basic
    AuthName "管理员登录"
    AuthUserFile /etc/apache2/.htpasswd
    Require valid-user
</Directory>
```

### 2. 限制IP访问

**Nginx配置**：
```nginx
location / {
    allow 192.168.1.0/24;  # 允许内网访问
    deny all;               # 拒绝其他所有IP
}
```

### 3. 使用HTTPS

确保后台管理系统使用HTTPS协议，保护数据传输安全。

---

## 📱 功能说明

### 主要功能模块

1. **登记记录管理**
   - 查看所有免费车辆登记记录
   - 搜索记录（按车牌号、免费原因等）
   - 查看记录详情
   - 删除记录
   - 导出Excel/PDF

2. **收费站管理**
   - 添加/编辑/删除收费站
   - 收费站编码和名称管理

3. **班组管理**
   - 添加/编辑/删除收费班组
   - 班组归属收费站
   - 班组编码和名称管理

4. **收费员管理**
   - 添加/编辑/删除收费员
   - 收费员归属班组
   - 工号和姓名管理

5. **监控员管理**
   - 添加/编辑/删除监控员
   - 监控员归属收费站
   - 工号和姓名管理

6. **班次设置**
   - 白班时间设置（默认：7:30-15:30）
   - 中班时间设置（默认：15:30-23:30）
   - 夜班时间设置（默认：23:30-7:30）
   - 支持自定义修改时间段

---

## 🎯 使用流程

### 初次使用

**1. 配置后台管理系统**
```bash
# 运行配置脚本
./scripts/setup-admin.sh

# 启动本地服务器
cd admin && python3 -m http.server 8080
```

**2. 访问后台管理系统**
```
浏览器打开：http://localhost:8080
```

**3. 初始设置**
- 添加收费站
- 在收费站下添加班组
- 设置班次时间（可选，已有默认值）

**4. 人员管理**
- 添加收费员到对应班组
- 添加监控员到对应收费站

**5. 记录管理**
- 小程序端登记的记录会自动显示
- 可以查看、搜索、导出记录
- 支持删除无效记录

---

## 🔧 故障排查

### 问题1：无法加载数据

**可能原因**：
- Supabase配置不正确
- 网络连接问题
- 数据库权限问题

**解决方案**：
```bash
# 1. 检查配置
cat admin/admin.js | grep "const SUPABASE"

# 2. 验证配置是否正确
cat .env | grep SUPABASE

# 3. 重新运行配置脚本
./scripts/setup-admin.sh
```

---

### 问题2：跨域错误（CORS）

**可能原因**：
- 直接打开HTML文件（file://协议）
- 浏览器安全策略限制

**解决方案**：
```bash
# 必须使用HTTP服务器访问
cd admin
python3 -m http.server 8080
# 然后访问 http://localhost:8080
```

---

### 问题3：导出功能不工作

**可能原因**：
- 浏览器阻止下载
- JavaScript错误

**解决方案**：
1. 检查浏览器是否允许下载文件
2. 打开浏览器控制台查看错误信息
3. 确保使用支持的浏览器（Chrome 90+）

---

## 📊 数据管理

### 导出数据

**导出Excel**：
1. 进入"登记记录"页面
2. 点击"导出Excel"按钮
3. 文件自动下载

**导出PDF**：
1. 进入"登记记录"页面
2. 点击"导出PDF"按钮
3. 文件自动下载

### 备份数据

**方法一：定期导出**
```bash
# 定期使用导出功能备份数据
# 建议每天或每周导出一次
```

**方法二：Supabase备份**
```bash
# 在Supabase控制台进行数据库备份
# 设置 -> 备份 -> 创建备份
```

---

## 🌟 最佳实践

### 1. 定期备份
- 每天导出Excel备份
- 每周在Supabase控制台创建数据库备份

### 2. 访问控制
- 部署在内网或添加访问控制
- 使用HTTPS协议
- 限制IP访问范围

### 3. 数据管理
- 定期清理无效记录
- 保持收费站和班组信息更新
- 及时更新人员信息

### 4. 性能优化
- 定期清理历史数据
- 使用搜索功能快速定位记录
- 避免同时打开多个后台管理页面

---

## 📞 技术支持

### 相关文档
- [后台管理系统README](admin/README.md)
- [项目完整文档](README.md)
- [部署检查清单](DEPLOYMENT_CHECKLIST.md)

### 常见问题
- 查看 [admin/README.md](admin/README.md) 的"常见问题"部分
- 检查浏览器控制台错误信息
- 验证Supabase配置是否正确

---

## ✨ 总结

### 快速访问步骤

1. **配置**：运行 `./scripts/setup-admin.sh`
2. **启动**：`cd admin && python3 -m http.server 8080`
3. **访问**：浏览器打开 `http://localhost:8080`

### 部署到线上

1. **Vercel**：`cd admin && vercel`
2. **Netlify**：`cd admin && netlify deploy --prod`
3. **传统服务器**：上传admin目录到Web服务器

### 安全建议

- ✅ 使用HTTPS协议
- ✅ 添加访问控制（基本认证或IP限制）
- ✅ 定期备份数据
- ✅ 不要泄露Supabase密钥

---

**后台管理系统已准备就绪！** ✅

现在你可以通过以上任一方式访问和使用后台管理系统了。
